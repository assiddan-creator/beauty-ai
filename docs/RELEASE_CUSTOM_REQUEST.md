# Free-form request release validation

This release branch exists only to trigger one Vercel preview for the free-form makeup request UX.

Validation target:
- clear dedicated free-form request box
- visible submit button next to the request
- requests run independently from presets and specific-product selection
- custom request no longer silently modifies preset generation or makeup-removal flow
